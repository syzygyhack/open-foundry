{
  "name": "@openfoundry/seed-nhs-acute",
  "version": "0.0.1",
  "description": "Synthetic NHS acute data seed generator",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist coverage",
    "generate": "tsx src/cli.ts"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
